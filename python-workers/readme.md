# Install library

```shell
pip install temporalio
pip install pytest
```

# Run the unit test

```shell
pytest
```

# Trigger the workflow

```shell
# start the worker
python run_worker.py


# start the workflow
python run_workflow.py
```
